POSTGRES_PASSWORD=

#DATABASE_URL="postgresql://postgres:postgres@localhost:5432/messme_dev"
# WS server URL — locally direct, in production via Caddy proxy leave empty
NEXT_PUBLIC_WS_URL="http://localhost:3003"
REDIS_URL="redis://localhost:6379"

SECRET_KEY=

# Web Push (VAPID)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
NEXT_PUBLIC_VAPID_PUBLIC_KEY=

# S3 Storage (firstvds)
S3_ENDPOINT=
S3_BUCKET=
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_REGION="us-east-1"

TURN_EXTERNAL_IP=
TURN_PASSWORD=

# AES-256-GCM key for server-side message encryption (64 hex chars = 32 bytes)
MESSAGE_ENCRYPTION_KEY=

# Primary admin email (gets admin access even if isAdmin=false in DB)
PRIMARY_ADMIN_EMAIL=matvey.bibin@mai.ru
